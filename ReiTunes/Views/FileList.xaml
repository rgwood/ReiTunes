<Page
    x:Class="ReiTunes.FileList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReiTunes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <controls:TreeView Name="FileTreeView" ItemsSource="{x:Bind ViewModel.FileTreeItems}">
            <controls:TreeView.ItemTemplate>
                <DataTemplate x:DataType="local:FileTreeItem">
                    <controls:TreeViewItem DoubleTapped="TreeViewItem_DoubleTapped" ItemsSource="{x:Bind Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind Name}" />
                        </StackPanel>
                    </controls:TreeViewItem>
                </DataTemplate>
            </controls:TreeView.ItemTemplate>
        </controls:TreeView>
    </Grid>
</Page>
