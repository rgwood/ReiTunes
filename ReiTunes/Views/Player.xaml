<Page
    x:Class="ReiTunes.Player"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReiTunes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <!--<Page.Resources>
        <StaticResource x:Key="MediaTransportControlsPanelBackground" ResourceKey="ApplicationPageBackgroundThemeBrush" />
    </Page.Resources>-->
    <StackPanel>
        <TextBlock
            Margin="7"
            HorizontalAlignment="Center"
            Text="{x:Bind ViewModel.SourceFileName, Mode=OneWay}" />
        <MediaPlayerElement
            x:Name="musicPlayer"
            AreTransportControlsEnabled="True"
            AutoPlay="True"
            Source="{x:Bind ViewModel.Source, Mode=OneWay}">
            <MediaPlayerElement.TransportControls>
                <MediaTransportControls
                    IsCompact="False"
                    IsCompactOverlayEnabled="False"
                    IsFastForwardButtonVisible="True"
                    IsFastForwardEnabled="True"
                    IsFastRewindButtonVisible="True"
                    IsFastRewindEnabled="True"
                    IsFullWindowButtonVisible="False"
                    IsZoomButtonVisible="False" />
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
        <AutoSuggestBox
            x:Name="SearchBox"
            Margin="10"
            HorizontalAlignment="Stretch"
            DisplayMemberPath="FullPath"
            QueryIcon="Find"
            QuerySubmitted="SearchBox_QuerySubmitted"
            TextChanged="SearchBox_TextChanged"
            TextMemberPath="FullPath" />
        <winui:TreeView Name="FileTreeView" ItemsSource="{x:Bind ViewModel.FileTreeItems, Mode=OneWay}">
            <winui:TreeView.ItemTemplate>
                <DataTemplate x:DataType="local:FileTreeItem">
                    <winui:TreeViewItem DoubleTapped="TreeViewItem_DoubleTapped" ItemsSource="{x:Bind Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind Name}" />
                        </StackPanel>
                    </winui:TreeViewItem>
                </DataTemplate>
            </winui:TreeView.ItemTemplate>
        </winui:TreeView>
    </StackPanel>
</Page>
