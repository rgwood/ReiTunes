<Page
    x:Class="ReiTunes.Views.FileTreeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ReiTunes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <winui:TreeView x:Name="FileTreeView" ItemsSource="{x:Bind ViewModel.FileTreeItems, Mode=OneWay}">
            <winui:TreeView.ItemTemplate>
                <DataTemplate x:DataType="local:FileTreeItem">
                    <winui:TreeViewItem DoubleTapped="OpenSelectedFileTreeItem" ItemsSource="{x:Bind Children}">
                        <TextBlock Text="{x:Bind Name}" />
                    </winui:TreeViewItem>
                </DataTemplate>
            </winui:TreeView.ItemTemplate>
        </winui:TreeView>
    </Grid>
</Page>